@extends("layouts.admin")
@section("page-title","TeacherAssignment")
@section("content")
<div class="container-fluid py-4"><div class="d-flex flex-wrap justify-content-between align-items-center mb-4"><div><h4 class="fw-bold mb-1"><i class="bi bi-person-check me-2"></i>TeacherAssignments</h4><nav aria-label="breadcrumb"><ol class="breadcrumb mb-0"><li class="breadcrumb-item"><a href="{{route('admin.dashboard')}}" class="text-decoration-none text-muted">Dashboard</a></li><li class="breadcrumb-item active text-gold">TeacherAssignments</li></ol></nav></div><a href="{{route('admin.teacher-assignments.create')}}" class="btn btn-gold"><i class="bi bi-plus-lg me-1"></i>Add</a></div>
@if(session('success'))<div class="alert alert-success alert-dismissible fade show"><i class="bi bi-check-circle me-2"></i>{{session('success')}}<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>@endif
<div class="card border-0 shadow-sm"><div class="card-body p-0">@if(isset($data)&&$data->count())<div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Teacher id</th><th>Classroom id</th><th>Subject id</th><th>Academic year id</th><th class="text-center">Actions</th></tr></thead><tbody>@foreach($data as $item)<tr><td class="ps-3 text-muted">{{$loop->iteration}}</td><td>{{$item->teacher_id ?? "—"}}</td><td>{{$item->classroom_id ?? "—"}}</td><td>{{$item->subject_id ?? "—"}}</td><td>{{$item->academic_year_id ?? "—"}}</td><td class="text-center"><a href="{{route('admin.teacher-assignments.edit',$item->id)}}" class="btn btn-sm btn-link text-primary p-0 me-2"><i class="bi bi-pencil"></i></a><form method="POST" action="{{route('admin.teacher-assignments.destroy',$item->id)}}" class="d-inline" onsubmit="return confirm('Delete?')">@csrf @method('DELETE')<button class="btn btn-sm btn-link text-danger p-0"><i class="bi bi-trash"></i></button></form></td></tr>@endforeach</tbody></table></div><div class="px-3 py-2">{{$data->links()}}</div>@else<div class="text-center py-5"><i class="bi bi-inbox text-muted" style="font-size:3rem;"></i><h5 class="mt-3 text-muted">No Records</h5></div>@endif</div></div></div>
@endsection